--- a/Install/EventScript
+++ b/Install/EventScript
@@ -15,7 +15,7 @@
 # NOTE:
 # shutdown calls will operate on stock system but will ignore parameters
 
-PATH=/usr/local/sbin:/usr/local/bin:/bin:/usr/bin:/sbin
+PATH=/usr/local/sbin:/usr/local/bin:/bin:/usr/bin:/sbin:/usr/sbin
 
 if [ "$1" = "C" ]; then
   TMP=$4
@@ -27,7 +27,7 @@
 
 ## Ensure tmp path is valid
 TMP=$(echo $TMP | tr -d '\r')
-if [ ! -d ${TMP} ]; then TMP=/tmp ; fi
+if [ ! -d ${TMP} ]; then TMP=/tmp/micro_evtd ; fi
 
 if [ "$DEBUG" ] && [ -d "$LOG" ]; then
   if [ "$DEBUG" -gt 0 ]; then
@@ -49,8 +49,8 @@
 
 create_script() {
 	## Create RAM version of control file
-	if [ ! -d $TMP/micro_evtd ]; then mkdir $TMP/micro_evtd ; fi
-	cp -a /usr/sbin/micro_evtd.event $TMP/micro_evtd/.
+	if [ ! -d $TMP ]; then mkdir $TMP ; fi
+	cp -a /usr/sbin/micro_evtd.event $TMP/.
 	## Stop flashing LEDs and clear error codes
 	micro_evtd -q -s 02520000,02540000
 }
--- a/Install/micro_evtd.sample
+++ b/Install/micro_evtd.sample
@@ -1,7 +1,7 @@
 # /etc/micro_evtd/micro_evtd.conf: Configuration file for micro_evtd
 
 # Directory that temporary files should be created in.
-# TMP=/tmp
+# TMP=/tmp/micro_evtd
 
 # Directory that the log file should be created in.
 # LOG=/var/log
--- a/micro_evtd.c
+++ b/micro_evtd.c
@@ -109,7 +109,7 @@
 char i_Use_Trend=1;
 char log_path[20]="/var/log";
 int iDebugLevel=1;
-char strTmpPath[20]="/tmp";
+char strTmpPath[20]="/tmp/micro_evtd";
 int i_FileDescriptor = 0;
 int i_Poll = 0;
 int mutexId=0;
